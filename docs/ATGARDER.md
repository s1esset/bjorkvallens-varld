# ÅTGÄRDER.md — rapporterade buggar som väntar på fix

Buggar som **ägaren har sett när hen spelat**, i väntan på `/fixa <id> <fel>` eller `/felsok <id>`.
Nyast överst. En rad per fel, inte per spel.

Status: ⬜ ej påbörjad · 🟨 pågår · ✅ fixad (rad flyttas till "Avklarat" med commit).

> Rutan **Första spår** är en *ledtråd från kodläsning*, inte en verifierad diagnos. `/fixa`
> ska alltid reproducera felet i harnessen först — ett plausibelt men falskt spår får inte
> generera en ändring. Se `.claude/commands/fixa.md`.

---

## Öppna

| # | Spel | Fel (som det rapporterades) | Första spår | Status |
|---|------|------------------------------|-------------|:--:|
| ~~#8~~ | `mata-munnen` | *"saker som spottas ut blir annan storlek och går ej att ta upp igen, blir förvirrande och plottrigt"* | `_spotta` återställer skalan med `v.scale.set(rec.view._fxRestScale?.x ?? 1, …)` (index.js:782) och gör sedan vyn till en fysikkropp i `_gorLos` (:407). Två frågor att avgöra med kod, inte gissning: hamnar `_fxRestScale` på fel värde (det pinnas i `DragController` ur `rec.base`, som läses ur `view.scale.x` vid `addItem`), och vad hindrar ett nytt grepp — `removeItem`, tappad `eventMode`/`hitArea`, eller `_snapHome` som skickar saken tillbaka till sin ursprungliga tallriksplats? ⚠️ Spelet skapar draget UTAN `skugga: true` (:185). | ✅ |
| ~~#9~~ | `mata-munnen` | *"saker har oxå en flytande skugga oavsett vart de är placerade, saker fastnat på ansiktet ska inte ha skuggan"* | `DragController._makeShadow` är opt-in och spelet slår inte på den, så skuggan ritas någon annanstans. Kandidater: `klet`-blobben i `_gegga` (:908, ritas i matens egen färg — en grå gaffels klet LÄSER som en skugga), en skugga inbakad i föremålets egen ritning, eller `foremal.js` `makeBoll({ skugga })` (:31). Måste lokaliseras till fil+rad innan något tas bort. | ✅ |
| ~~#10~~ | `mata-munnen` | *"busiga saker / mat som fastnar på ansiktet ska ha sekundära utseenden / former så kladdiga saker ser utkladdade och saker ska 'fastna' mer i hur de sitter på ansiktet"* | `_gegga` (:922–927) ritar maten REN: `bit.addChild(rec.data.vy())` med `scale 0.62` och en slumpvinkel — samma konstverk som på tallriken, bara mindre. ⚠️ **Pröva premissen:** CLAUDE.md säger att mat inte gick att DEFORMERA (5–7 lagrade `Graphics` per rätt, ingen silhuett, `generateTexture` förbjudet). Ett "sekundärt utseende" måste alltså byggas som en BEHANDLING (hoptryckning + kladd + rinn) eller som egna ritningar — inte som en töjning. | ✅ |
| ~~#11~~ | `mata-munnen` | *"vätska ska kunna spillas ut mer samt kunna hållas över ansiktet"* | `_spill` (:477) anropas i dag BARA ur `_gorLos` (:433), alltså när en vätskebärare blir en lös kropp. Det finns ingen väg att hälla medan man håller i saken. Vätskevärldens `bounds.top` är 150 och ansiktet spänner y≈33–395 (`ANS` = {620, 268, h 470}, `KANT_Y` 395), så att hälla över ansiktet ryms i världen som den står. | ✅ |
| ~~#12~~ | `mata-munnen` | *"när man matar chili kan man göra ansiktet mer rött och göra en simpel animation där rök kommer ut ur öronen"* | Kroken finns: sväljningen kallar minen `het` för chili (`MIN_PER_MAT.chili`, :68) i återanropet på :836. Ansiktet är `Sprite`-lager i `lib/ansikte.js` (:78–91) utan något färg-API — en `rodna(t)`/tint-metod behöver läggas till i riggen (delad kod → `test:all`). Öronen har ingen egen punkt i manifestet; de får härledas ur `bredd`/`_ogonY`. | ✅ |

## Verktygsfynd

Hittade av harnessen/bildkollen, **inte** rapporterade av ägaren — därför i egen tabell. Samma
regel gäller: reproducera innan du ändrar.

| # | Var | Fynd | Bevis | Status |
|---|-----|------|-------|:--:|
| V16 | `src/lib/` + 89 spelfiler (251 anrop) | **`destroy({ children: true })` river INTE en `Graphics` egen `GraphicsContext`.** Pixi v8:s `Graphics.destroy(options)` friar den ägda kontexten bara i två fall: `destroy()` helt utan argument, eller `options === true` / `options.context === true`. Ett options-OBJEKT (`{ children: true }`) faller mellan grenarna — första grenen kräver `!options`, andra kräver `context === true` — och `_ownedContext` sätts sedan till `null` utan att ha rivits. Repot använder `destroy({ children: true })` på **251 ställen i 89 filer**, alltså i praktiken varje spel vid varje rivning. Fyndet kom ur `roliga-snurran`s bygge, där en lokal `riv()`-hjälpare skrevs för att komma runt det. **Ändra ingenting förrän blastradien är MÄTT** — mekanismen är bekräftad i källan, men hur mycket GPU-minne som faktiskt blir kvar per spelbyte är inte mätt, och en global ändring rör varje spel i sviten på en gång (samma resonemang som V10). Nästa steg: mät minnet över N spelbyten med och utan `{ context: true }`, och lyft i så fall en `rivNod()` till `lib/` i stället för att ändra 251 anrop för hand. | `node_modules/pixi.js/lib/scene/graphics/shared/Graphics.mjs:145-153` läst direkt: `if (this._ownedContext && !options) { … } else if (options === true \|\| options?.context === true) { … }` följt av `this._ownedContext = null`. `grep -rn "destroy({ children: true })" src/` → **251 träffar i 89 filer**. GPU-effekten är ÄNNU INTE mätt. | ⬜ |
| ~~V15~~ | ~~`glittergrottan` + `src/lib/three3d.js`~~ | **✅ STÄNGD 2026-08-12 — se raden i Avklarat.** Frågorna (a) och (c) är besvarade med mätning; (b) krävde en omstart av maskinen och behövdes aldrig. ~~**`glittergrottan` faller ~30 % av gångerna ÄVEN ENSAM — och det är en kontext som inte kan SKAPAS, inte en som förloras.** Fyndet dök upp i ett fullt svep (71/72) och såg först ut som V14b:s signatur, men det är en annan mekanism och den viktigaste skillnaden är att **parallell last inte behövs**: uppmätt **3 fall av 10 ensamma körningar**. Konsolen säger `THREE.WebGLRenderer: A WebGL context could not be created` ×2 följt av `Error creating WebGL context.`, och därefter `tom-scen` (0,0 % innehåll). Pixis EGEN kontext fungerar hela tiden — det är den ANDRA kontexten som fallerar, och `glittergrottan` är svitens enda spel som behöver två. **Diagnos ur koden, inte gissad:** `three3d.js` har redan en delad renderare med `try/catch` (rad 66–86), men fångsten hjälper bara när `_sharedRenderer` REDAN finns — `if (!_sharedRenderer) throw err`. I harnessen är varje körning en färsk webbläsare, alltså är första monteringen alltid `_sharedRenderer === null`, och då kastas felet vidare och spelet dör vid start. Skyddet täcker med andra ord återinträdet men inte första försöket, vilket är precis det fall testet alltid kör. **Inte attribuerbart till v1.149–1.150:** `glittergrottan` importerar bara `pixi.js`, `gsap`, `feedback.js`, `theme.js` och `swedish.js` — ingen av dem rördes, så armarna är byte-identiska för det spelet. **Kvar att avgöra innan något ändras:** (a) är 30 % nytt i dag eller har det alltid funnits och bara maskerats av att svepen råkat vinna? (Föregående session loggade 72/72 i två fulla svep.) (b) är det maskinens GPU-tillstånd efter en dag med många playwright-instanser — 0 chrome-processer levde vid mätningen, så det är inte kvarliggande processer. En omstart av maskinen är det billigaste sättet att skilja (a) från (b). (c) om felet är äkta: ett andra försök att skapa renderaren efter en bildruta, eller att låta spelet gå vidare utan 3D-lagret i stället för att dö. | 10 ensamma `npm run test glittergrottan`: **✗✓✗✓✓✗✓✓✓✓ = 3 fall av 10**. Ett fullt `test:all` 71/72 med samma spel rött. `.test-logs/glittergrottan.json`: `konsolfel ×3` (tre WebGL-rader) + `tom-scen ×1` (0,0001 % innehåll, bakgrund `#f8f8f8`). Chrome-processer vid mätningen: **0**.~~ | ✅ |
| V14b | `scripts/test-game.mjs` (harnessen) | **Hypotesen i V14 är MÄTT FALSK, och harnessen bär nu sin egen diagnos.** V14 stod i tre svep på att `golvet-ar-lava` har svitens tyngsta montering (enda spelet med BÅDE `FluidWorld`/`FluidView` OCH full `createScene`) och att skärmdumpen därför hinner före första målade bildrutan. Ny sond **`scripts/_montageprobe.mjs`** mäter monteringskostnaden som **längsta gapet mellan två `requestAnimationFrame`** efter navigeringen (`nav.go()` är asynkron — en synkron mätning av anropet självt visar ~0 och säger ingenting). Utfall över alla 72 spel, CPU 4× strypt, median av 3 varv: **`golvet-ar-lava` 16,8 ms = svitens median (1,0×)**, alltså EN bildruta, precis som 69 andra spel. De enda som sticker ut är `pizzabageriet` **50,0 ms (3,0×)** och `hamburgerbygget` **33,4 ms (2,0×)** — och ingen av dem är spelet som faller. Dessutom: skärmdumpen tas efter tryck, drag OCH 900 ms, alltså flera sekunder efter monteringen — monteringskostnad kan omöjligt förklara den. **Vad som byggdes i stället för en fix på en död hypotes:** (1) omtagningen höjd från EN till **upp till tre**, (2) en **vakt för `webglcontextlost`/`webglcontextrestored`** som sätts före spelet monteras — en förlorad GL-kontext ger en helt tom duk utan ett enda konsolfel och är en av få mekanismer som kan tömma en duk som REDAN varit målad, (3) när en bild är tom samlas bevis i **samma ögonblick** (gl-kontext förlorad? gl-händelser? antal barn på stage och i världen? dukens storlek? `visibilityState`?) och (4) om omtagningen löser det rapporteras en **varning `tom-bild-omtagen`** med hela diagnosen i stället för tystnad. Vägen är självtestad, inte hoppad: `--tvinga-tom N` låtsas att de N första bilderna var tomma → `gl-kontext levande, stage 2 barn, varld 3 barn, duk 1280x720, sida visible`. Nästa träff kommer alltså med sin orsak. **✅ TRÄFFEN KOM 2026-08-11, och den bar sin orsak: `tom-bild-omtagen ×2 — gl-kontext FORLORAD (forlorad), stage 2 barn, varld 3 barn, duk 1280x720, sida visible` i `tvatta-djuret`.** Det är precis den mekanism vakten byggdes för, nu MÄTT i stället för gissad: scenen var byggd (stage och värld hade sina barn, duken hade rätt storlek, sidan var synlig) — det var **WebGL-kontexten som försvann**, vilket tömmer en redan målad duk utan ett enda konsolfel. Omtagningen räddade bilden, så svepet blev 72/72 och fyndet landade som en VARNING med diagnos i stället för ett rött `tom-scen`. Två följder: (1) hypotesen om monteringskostnad är därmed dubbelt begravd — det här händer långt efter monteringen, (2) fyndet hörde till `tvatta-djuret` den här gången och till `golvet-ar-lava` svepet före, alltså **vandrar det igen** precis som V12b:s signatur. Kvar att avgöra: varför tappas kontexten? Fyra parallella Chrome-instanser med WebGL på en maskin är den uppenbara misstänkta (GPU-processens minne), och i så fall är det en egenskap hos SVITEN, inte hos något spel. | `_montageprobe.mjs` (72 spel, `--cpu 4 --varv 3`): `golvet-ar-lava` 16,8 ms mot medianen 16,9 ms · topp: `pizzabageriet` 50,0 · `hamburgerbygget` 33,4 · `spindelhjalten` 17,3. `test:all` efter harnessändringen: **72/72 gröna, noll `tom-scen`, noll `tom-bild-omtagen`** (svepet träffade inte flaket — frekvensen är ~1 av 7). | 🟨 |
| ~~V14~~ | ~~`scripts/test-game.mjs` (harnessen)~~ | ~~**`tom-scen` ÖVERLEVER V12b-fixen — sällsynt, men den finns kvar.**~~ Ersatt av V14b: observationen står, men hypotesen om orsaken är mätt falsk. V12b:s rad påstår "72/72 gröna med noll fel-nivåfynd" efter att skärmdumpen började vänta på två `requestAnimationFrame` + en omtagning. Det stämmer inte längre: ett fullt svep 2026-08-10 loggade `tom-scen ×1` på `golvet-ar-lava` (0,0 % innehåll) medan sviten ändå var 72/72 grön. Frekvensen är ungefär **1 av 7 fulla svep**, alltså mycket lägre än före fixen (3 av 3), men inte noll. Praktisk följd: ett ensamt `tom-scen` i ett fullt svep är INTE i sig bevis för en regression — men det får heller inte avfärdas utan mätning. **UPPDATERAT 2026-08-10 (sen natt III–IV): fyndet FLYTTAR SIG INTE LÄNGRE — det koncentreras till `golvet-ar-lava`.** Under ett långt platthetspass kördes **åtta fulla svep**: sex rena, två med `tom-scen`, och BÅDA på `golvet-ar-lava`. Tillsammans med V14:s ursprungliga observation är det tre av tre på samma spel. Det är en skärpning av hypotesen, inte en motsägelse: V12b:s signatur var att fyndet vandrade mellan spel, men `golvet-ar-lava` är svitens enda spel som BÅDE monterar en `FluidWorld`/`FluidView` (filter, hundratals sprites) OCH en full `createScene` — alltså den tyngsta monteringen i sviten, vilket är precis vad "skärmdumpen hinner före första riktiga bildrutan" förutsäger. **Uteslutet med kod, inte gissning:** spelet anropar `createScene` med `ground: false` (`index.js:110`), så `scene.js`-markens toning (`6789698`) ligger inte i dess kodväg — `showGround`-grinden hoppar över hela blocket. Nästa steg om det ska stängas: mät monteringstiden för `golvet-ar-lava` mot svitens median och höj omtagningen från EN till tills-bilden-inte-är-tom för spel över en viss monteringskostnad. | **A/B-svep, 3 rundor växelvis, hela sviten** (`scripts/_ab.sh`) mot de fem spel som ändrats i samma omgång: **HEAD (ny kod) 72/72 rent 3 av 3 · ÄNDRING (gammal kod) 72/72 rent 3 av 3.** Sex fulla svep utan ett enda fynd i någon arm, plus `golvet-ar-lava` ensamt rent 3 av 3. Fyndet är alltså varken attribuerbart till ändringen eller reproducerbart på begäran. Att BÅDA armarna är rena är själva poängen: hade bara min arm mätts hade tystnaden lika gärna kunnat läsas som tur. | ✅ |
| ~~V13~~ | ~~`scripts/_vevprobe.mjs`~~ | **Två glapp-mått var röda på HEAD — måttet rapporterade den ENSKILT sämsta bildrutan, inte vad handen känner.** Fyndet såg ut som en maskinberoende tröskel (samma kod mätte 12°/17° när `0da667d` skrevs, 19°/23° och 11°/22° senare på samma maskin). Orsaken var sonden själv: den för fram fingret 0,18 rad (10,3°) och mäter glappet efter EN `requestAnimationFrame` — men spelets ticker hinner inte alltid köra i det fönstret, så de olyckliga bildrutorna bär ett helt oservat fingersteg. `Math.max` över 30 rutor hittar alltid en sådan. | **Mätt, inte gissat:** medianen och värstavärdet delades isär och kördes fyra gånger. Tom vev **median 11° = värsta 11°** (helt stabil). Femhjulsbygge **median 11–13° men värsta 21–22°** — och spridningen 10° är just ett oservat fingersteg (0,18 rad = 10,3°). Måttet läser nu medianen; fyra körningar i rad gröna och inom 2°. Sidofynd värt att minnas: bygget och den tomma veven har i praktiken **samma** jämviktsglapp (11° mot 12°), alltså ≈ ett fingersteg — framkopplingen i `0da667d` gör kopplingen så nära bildrutperfekt som den kan bli. | ✅ |
| V12b | `scripts/test-game.mjs` | **`tom-scen` var harnessens eget mätfel — åtgärdat.** Fyndet flyttade sig: `tvatta-djuret` två gånger, sedan `flipperspel` och `folj-sparet`, medan varje spel var grönt när det kördes ensamt. Orsak: skärmdumpen togs efter en TIMER, så `page.screenshot()` kapplöpte med WebGL-rutan och under parallell last (fyra webbläsare) vann den ibland — bilden blev helt tom och `bildkoll` fällde ett fel-nivåfynd på ett spel som mådde utmärkt. **Fix:** vänta på två `requestAnimationFrame` (minst en målad ruta) före bilden, plus EN omtagning om bilden ändå blev tom. En scen som verkligen är tom är tom två gånger, så omtagningen kan aldrig dölja ett riktigt fel. | Före: `tom-scen` i 3 av 3 fulla svep (olika spel varje gång). Efter: **72/72 gröna med noll fel-nivåfynd**, och de tre drabbade spelen gröna var för sig. | ✅ |
| ~~V12~~ | ~~`tvatta-djuret` (i parallell svit)~~ | ~~**`tom-scen` återkommer på SAMMA spel.**~~ Ersatt av V12b — fyndet visade sig inte höra till spelet alls. Två av två fulla `test:all`-körningar 2026-08-10 loggade `tom-scen ×1` i `tvatta-djuret` — helt tom cremeplatta, 0,0 % innehåll, inga konsolfel, och sviten ändå 72/72 grön. Ensam är spelet grönt **6 av 6 gånger** och ritar hela scenen. Att det är samma spel båda gångerna talar emot ren slump: hypotesen är att spelets montering är TUNG och att harnessens skärmdump under parallell last hinner före första riktiga bildrutan — alltså en tidsberoende mätning, inte en trasig scen. | Två `test:all` (båda 72/72 gröna, båda med fyndet) · 6 ensamma körningar gröna · **A/B-svep över 6 armar helt rent** (V11), vilket utesluter `plask-i-vattnet`s vätskefilter som orsak. | ✅ |
| V10b | `PhysicsWorld._make` (opt-in byggd) | **`{ isStatic: true, studs: 0.75 }` finns nu — den form V10 rekommenderade, byggd och mätt.** `_make` sätter restitution EFTER `Body.setStatic` (och uppdaterar `_original`, så en kropp som senare väcks behåller sin studs), loopar över `parts` som matter själv gör, klämmer till 0..1 och strippar nyckeln ur matters options så den inte hamnar i matters namnrymd (samma fälla som `mat` i B4). **Ingen global återställning** — 50 tal i 19 spel är fortfarande nollade, precis som förut, eftersom en global fix hade väckt 25 kombinationer på en gång i handtrimmade spel. `npm run check` skriver nu EN sammanfattningsrad över talen (`-- --studs` ger hela listan) i stället för att låta dem ligga tysta; det är migreringslistan. **Två gränsdragningar som listan själv tvingade fram:** `restitution: 0` räknas inte (det säger vad `setStatic` ändå gör), och raden säger *"medan kroppen är statisk"* — `kulbana:140` skapar spelets KULA statisk med 0,42 och får tillbaka talet ur `_original` när den väcks, alltså är listan en läslista, inte en fixlista. ✅ **FÖRSTA KUNDEN HAR LANDAT 2026-08-11: `flipperspel`s stolpar** (ägaren valde spelet i sitt speltest). Genomgången gav ett annat svar än frågan förutsatte och är värd att läsa innan nästa kund väljs: **sex av spelets åtta statiska tal kan aldrig väckas, av ett ANDRA oberoende skäl** — parets regel är `max(A,B)` och kulan bär **0,62**, så `wall` 0,3/0,4 · `sling` 0,5 · `spinner` 0,55 · `flipper` 0,3 hade förlorat mot kulan även utan `setStatic`. Kontrollmätning: ett statiskt 0,5 ger 59,4 px hopp både nollat och väckt, **0,0 px skillnad**. Kvar över kulan: `peg` 0,7 och dynorna 0,68–0,82 — och **dynorna har en egen impuls** (`_kickOff`), alltså exakt den dubblering V10b varnade för. Stolpen var spelets enda yta som både låg över kulan och saknade egen impuls. **MÄTT** (`_flipperprobe.mjs`, naken fysik med spelets geometri): hoppet **59,4 → 75,4 px (+27 %)**, parets studs **0,62 → 0,70**, listan **50 → 49**. ⬜ **Kvar:** dynorna ger **+26,8 px** om de väcks — ägarbeslut, kräver att någon spelar bordet. ⚠️ **Lärdom som gäller varje framtida kund: den rörliga kroppens EGET studstal är ett golv.** Läs det först; ligger den statiska ytans tal under, är `studs` fel verktyg oavsett hur avsiktligt talet ser ut. | `node scripts/_studsprobe.mjs` **13/13 gröna** (6 nya mått): `studs: 0.9` ger 143,3 px hopp mot den simulerade fixens 143,3 (identiskt) och mot 4,7 px utan opten — **+139 px**. UTAN opten står kroppen på `restitution 0` och hoppar 4,7 px, alltså är de 50 talen orörda. `_original.restitution` bär 0,9 · `body.studs === undefined` · `studs: 2 → 1` · `studs: 0` är ett medvetet dött golv. `test:all` 72/72. | 🟨 |
| V10 | `PhysicsWorld._make` (18 spel, 44 tal) | **`restitution` på en STATISK kropp är en nullhandling.** `_make` skapar kroppen dynamisk och sätter den statisk efteråt (medvetet — NaN-fixen vid rad 213), och matters `Body.setStatic` **nollar då `restitution` och sätter `friction` till 1**, med originalen undanlagda i `body._original`. Varje `restitution` ett spel satt på en ramp, vägg, studsplatta eller hink har alltså aldrig gjort något. | **BLASTRADIEN ÄR NU MÄTT** (`node scripts/_studsprobe.mjs`, 6 mått, ingen webbläsare) och den är mycket mindre än raden ovan påstod. Tre fynd: **(1) parets regel är `max(A, B)`** — uppmätt 0,90 mot 0,30 → 0,90 (medel hade gett 0,60, produkt 0,27). Alltså är **varje statiskt tal som ligger PÅ ELLER UNDER den rörliga kroppens egen studs en ren no-op**: 35 av 60 mätta kombinationer rör sig **0,0 px**. **(2) De 25 som ändras gör det mycket:** en `heavy`-kropp (0,18) mot en 1,00-platta går från 5 → 176 px hopp; 0,50 → 44, 0,70 → 87, 0,92 → 150. **(3) VÄRLDSVÄGGARNA BERÖRS INTE** — `_buildWalls` skickar `{isStatic, restitution: 0.4}` rakt in i `Bodies.rectangle` och lägger kroppen med `Composite.add`, alltså aldrig genom `_make`; matter slutar där med `_original === null`, så 0,4 är **borta, inte undanlagt**. "Varje vägg i varje fysikspel blir studsig på en gång" var det tyngsta argumentet mot fixen, och det **kan inte hända**. | 🟨 |

> ### V10b — underlag för NÄSTA kund *(nattköns N13, skrivet 2026-08-12 — enbart kodläsning,
> ingen speländring)*
>
> Fyra kandidater lästes mot koden med `flipperspel`-lärdomen som filter: **den rörliga
> kroppens eget studstal är ett GOLV** (parets regel är `max(A,B)`), och **en yta som redan
> lägger på en egen impuls får inte också få `studs`** — det blev en dubblering på dynorna.
> Resultatet: två av de fyra faller på de två reglerna, och rangordningen blev en annan än
> den kön gissade.
>
> **① `bowling` — starkast, och det är inte studsen som är fyndet.** Klotet är
> `MATERIALS.heavy`, alltså golv **0,18** (`index.js:113`). Spelet har **ingen
> kollisionshanterare alls** — inget `_onCollision`, ingen egen impuls någonstans — så
> dubbleringsrisken är noll. Lanväggarnas 0,2 (`:432-433`) ligger i praktiken på golvet och
> är inte värt en ändring. Men **kantstöden deklarerar 0,75** (`:589-590`), och de är
> spelets TILLGÄNGLIGHETSHJÄLP: hela poängen med "Kantstöd PÅ" är att klotet ska studsa in
> mot käglorna i stället för i rännan. Effektivt studsar de på **0,18**. Och värre:
> `_previewBounds` matar `predictTrajectory` med **`restitution: 0.75` när kantstödet är
> på** (`:441`) — **den prickade banförhandsvisningen lovar alltså en studs som är drygt
> 4× den klotet faktiskt får**, i ett spel vars egen filhuvud påstår att pricklinjen
> matchar den verkliga studsbanan "till ~några px" (`:15-18`). Det är inte en känsla, det
> är ett sikte som ljuger, och bara för barn som behöver hjälpen. **Föreslaget värde:
> `studs: 0.75`** — inte valt för att det känns rätt, utan för att det är talet
> förhandsvisningen redan ritar; då blir sikte och verklighet samma sak utan att någon
> behöver trimma om banorna.
>
> **② `spindelhjalten` — äkta men litet.** Hjälten är `MATERIALS.bouncy`, golv **0,86**.
> `_onCollision` (`:895`) gör bara ljud, `_heroOof`, `puff` och `pop` — **ingen egen
> impuls**, alltså ren väg. Världsväggarna kommer från `PhysicsWorld({ walls: [...] })` och
> är oberörda av V10b (0,4, dessutom under golvet). Kvar: **studsknoppen/studsmolnen på 1,0**
> (`:328`) — spelets enda tal över golvet. Lyftet är alltså 0,86 → 1,00, mycket mindre än
> `flipperspel`s stolpe fick. ⚠️ Ett SEPARAT fynd på vägen, som inte är V10b: `BOUNDS`
> matar förhandsvisningen med **0,72** (`:30`) medan hjälten i verkligheten bär 0,86 mot
> golv och väggar — pricklinjen **under**lovar. (Att den inte känner studsknoppen alls är
> däremot avsiktligt och dokumenterat i koden, `:782-785`.)
>
> **③ `rulla-bollen-hem` — bara ETT smalt fall, resten stängt av de två reglerna.**
> Bollens studs skrivs om per bana (`_applyMaterials:651`): normal **0,55** · studsboll
> **0,82** · tung boll **0,40**. Dynorna (0,92, `:533`) **har en egen radiell impuls**
> — `boost = min(fart × 1,1 + 1,5, 26)` (`:986`) — alltså exakt `flipperspel`-dynans
> dubblering, och de ska lämnas. Klossarna (0,32) ligger under varje boll. Väggarna (0,55)
> ligger PÅ golvet för normalbollen och UNDER för studsbollen. Enda kombinationen som
> faktiskt skulle ändras är alltså **tunga bollen (0,40) mot väggen (0,55)**. Och just den
> är dessutom spelets enda pricklinje-lögn: `Math.max(ball.rest, WALL_REST)` (`:652`) —
> koden härmar matters parregel korrekt, men den räknar med ett väggtal som `setStatic` har
> nollat, så tunga bollen får en pricklinje ritad på 0,55 och en verklighet på 0,40.
>
> **④ `flipperspel` — klar** (stolpen, `43d71b4`). Enda öppna resten är dynornas +26,8 px,
> som står som ett ägarbeslut i raden ovan.
>
> **Vilken sond skulle vakta bytet?** `scripts/_studsprobe.mjs` finns redan och mäter naken
> fysik utan webbläsare — den är rätt verktyg för ①, eftersom `bowling`s kantstöd är två
> raka statiska rektanglar och klotets skottfart sätts med `Body.setVelocity`: en arm med
> kantstöd PÅ, skott mot vänsterkanten, och måttet är **klotets x-läge när det når
> käglornas y** mot vad `predictTrajectory` ritade med samma indata. Att sikte och utfall
> går ihop är hela fyndet, alltså är det också måttet. ⚠️ Kör den mot HEAD först — röd sond
> har varit sondens eget fel nio gånger i det här repot.
>
> **Ägarnära kvar:** vilket av ① och ② som ska byggas. ① är den enda av de fyra där en
> funktion barnet kan slå PÅ inte gör vad den utger sig för att göra.

> **Lärdomen från V12 → V12b är värd mer än fixen.** Fyndet såg i två svep ut att höra till
> `tvatta-djuret` — samma spel båda gångerna, vilket "talar emot slump". Det gjorde det
> också: mönstret var inte slumpmässigt, men det hörde till HARNESSEN, inte till spelet.
> Först när ett tredje svep la fyndet på två helt andra spel blev orsaken synlig. **Ett
> återkommande fynd på samma plats är inte bevis för att platsen är orsaken** — och
> hade jag "fixat" `tvatta-djuret` efter två svep hade jag ändrat ett friskt spel och ändå
> haft kvar felet.

> **Så A/B:ar man något som redan är committat** (gjordes för V11). `_ab.sh` bygger sina armar
> av *ostagade* ändringar, så lägg den GAMLA filen i arbetskopian —
> `git checkout HEAD~1 -- <fil>` — och kör svepet på samma fil. Armarna byter då etikett:
> raden **HEAD** är den nya koden och **ANDRING** är den gamla. Städa efteråt med
> `git checkout HEAD -- <fil>` och kontrollera att `git stash list` är tom.

> **V10: så bör fixen se ut, efter mätningen (`_studsprobe.mjs`).** En global "återställ
> `_original` i `_make`" är fortfarande fel form — inte för att den är farlig överallt, utan
> för att den ändrar **25 kombinationer på en gång** i spel som är handtrimmade mot dagens
> beteende. Men mätningen delar upp arbetet i två högar, och den ena är tom:
> · **Dött på riktigt** (statiskt tal ≤ den rörliga kroppens studs): `glasstornet` 0,05 ·
>   `knuffa-tornet` 0,05 · `plask-i-vattnet` 0,04 · `kulbana` 0,1–0,2 · `natskott-pa-stan`
>   0,18–0,2 · `magnet-fiske` 0,3 · `fanga-frukten` 0,35. De kan aldrig regressa — talen kan
>   lika gärna **strykas** som vilseledande, utan en enda mätning.
> · **Kandidater som faktiskt skulle vakna:** `spindelhjalten` 1,0 · `rulla-bollen-hem` 0,92 ·
>   `bowling` 0,75 · `flipperspel` 0,5–0,7 · `studsbollar` 0,55 · `bajs-och-kiss`,
>   `mata-monstret`, `spindelnatet`, `studsa-ner`, `vippbradan` 0,5 · `enhorningen-elvira` 0,6.
>   Flera av dem är uppenbart AVSIKTLIGA och har aldrig fungerat (en flipperkudde som inte
>   studsar, en bowlingbumper som inte studsar) — där är fixen en förbättring, inte en risk.
> **Rekommenderad form: opt-in per kropp**, t.ex. `{ isStatic: true, studs: 0.75 }` som `_make`
> återställer, i stället för att väcka alla 44 tal samtidigt. Då migreras ett spel i taget med
> en sond per spel, och de döda talen kan strykas direkt. **Kvar att göra av C1:** genomgången
> spel för spel av vilka tal som är avsiktliga — den kräver att man SPELAR spelen, och är
> därför en ägarnära uppgift snarare än en mätning.

> **V10 var medvetet inte fixad i samma svep som den hittades.** Att återställa `_original` efter
> `setStatic` vore två rader, men det ändrar fysiken i 23 spel samtidigt — varje ramp, vägg och
> hink som bär ett `restitution`-tal skulle plötsligt börja studsa, och flera av dem är
> handtrimmade mot dagens beteende (exakt `mat()`-fällan: skriv ut talen du vill BEHÅLLA).
> Fixen kräver A/B över hela sviten (`scripts/_ab.sh`) och en genomgång spel för spel av vilka
> tal som är avsiktliga. `kulbana` behövde ingen sådan fix: en fjäderbräda (`lib/fjader.js`) ger
> en riktig studs oberoende av matters restitution.

> ⚠️ **En sond ger ledtrådar, inte domar** — den läser text, inte beteende, och rapporterar den
> FÖRSTA vakten i en hanterare. Reproducera i harnessen innan du ändrar. Fixmönstret när fyndet
> är äkta: `kvittera(ctx.fxLayer, x, y, ctx.services.audio)` ur `lib/feedback.js`.

## Avklarat

| # | Var | Fel | Fix | Commit |
|---|-----|-----|-----|--------|
| 14 | `src/lib/pwa.js` | **"Jag har behövt uppdatera appen från huvudmenyn genom att alltid göra det 2 gånger i rad för att den ska uppdateras."** *(ägarens speltest 2026-08-21)* | **Felet satt i vite-plugin-pwa, inte i vår logik — och det var osynligt tills källan lästes.** I prompt-läge läggs omladdningslyssnaren först **inne i** `showSkipWaitingPrompt`, som körs på workbox `waiting`-händelse — alltså exakt samma stund som `onNeedRefresh` sätter vår `pending`-flagga (`node_modules/vite-plugin-pwa/dist/client/build/register.js`). **Första** trycket, när ingenting väntade ännu, skickade därför SKIP_WAITING utan att någon lyssnade på `controlling`: servicearbetaren bytte, men sidan laddades aldrig om. Strax efter kom `waiting`, `pending` blev sann, och **andra** trycket gick den redan fungerande vägen. Andra halvan: `waitInstalled(fresh, 4000)` gav installationen **4 sekunder** — bygget precachar **1 911 filer / 35 MB**, så gränsen löpte i praktiken alltid ut och SKIP_WAITING gick till en arbetare som fortfarande installerade. **Fix:** `forceUpdate()` äger nu hela kedjan — fråga servern → vänta in att arbetaren blir `waiting` (60 s, med avin "Laddar ner uppdateringen…") → skicka SKIP_WAITING själva → ladda om på `controllerchange`, med en tidsgräns som skyddsnät. Hittas ingen ny version laddas sidan **inte** om längre (den gamla `fallbackReload()` fanns bara för att knappen skulle "kännas som att den gör något" — men en oväntad omstart är dyrare än en avi). **`scripts/_uppdatprobe.mjs` (ny).** Mätningen går inte att göra på dev-servern — där finns ingen service worker — så sonden kör mot ett **riktigt bygge** på `localhost:4173` (localhost är en säker kontext, precis som Pages), bygger om appen med en ny version medan sidan står uppe, och upprepar ägarens ritual tills sidan laddar om. Växelvis: **HEAD krävde 2 rundor, fixen 1** — och det nya bygget körs verkligen efteråt (entry-chunken `index-DkOOqngJ.js` → `index-BGwsrB8B.js`; utan det måttet mäter man bara att sidan laddade om). **Kontrollarm utan ny version: HEAD laddade om ändå** (en falsk omstart), fixen rör sig inte. 0 konsolfel i båda. | `2bb9ead` |
| 15 | `src/shell/screens/MenuScreen.js` | **"Man ska inte behöva hålla in en knapp för att uppdatera, det ska kunna gå på ett enkelt tryck."** *(ägarens speltest 2026-08-21)* | Versionspillen låg bakom **både** föräldragrinden (håll 2,5 s) och en bekräftelsedialog — fyra moment för en handling föräldern ändå gjorde två gånger i rad. Båda borttagna: **ett tryck**. P0 GRIND räknar upp inställningar/avsluta/ta bort/nollställ/länkar, och en uppdatering är inget av det — den tar inte bort något, nollställer ingenting, och spardatan ligger kvar (`save.flush()` körs före). Träffar ett barn pillen händer i värsta fall en omstart, och står appen redan på senaste versionen laddas sidan inte ens om. En spärr hindrar två samtidiga sökningar. **`scripts/_grindprobe.mjs` arm 6:** HEAD öppnade grinden 1 gång och kallade `forceUpdate` **0** gånger; efter fixen grind **0**, `forceUpdate` **1**. | `2bb9ead` |
| 16 | `src/services/ParentalGate.js` | **"Avbryt-knappen på föräldra-låset är för liten och svår att se/träffa, barnet blir fast på den skärmen."** *(ägarens speltest 2026-08-21)* | **Uppmätt: träffytan var 112×28 px** — 28 px höjd mot P0:s ≥96, och i dämpad `inkSoft` mot creme syntes den knappt. Den var en bar `Text`, inte en knapp. Värre: bakgrunden var `static` men hade **ingen** hanterare, så den fångade trycket utan att göra något — ett barn som råkat öppna grinden hade ingen väg ut alls (håll-knappen leder inte ut). **Fix:** avbryt är nu en riktig `Button`, **300×112 px** i rött, och kortet gjordes 40 px högre med knoppen flyttad upp så P0:s 24 px mellan träffytor håller. Tryck **utanför** kortet stänger också. ⚠️ Kortet behövde `eventMode = static`: Pixi v8:s standard är inte hittestad, så ett tryck mitt på kortet hade fallit rakt igenom till bakgrunden och stängt grinden. **`scripts/_grindprobe.mjs` (ny, + skärmdump — en grind går inte att bedöma i tal):** yta 112×28 → **300×112** · tryck utanför kortet stängde **inte** på HEAD, stänger nu · **kontrollarm:** tryck MITT PÅ kortet stänger inte i någon arm · och grinden är inte försvagad — håll 0,6 s släpper inte igenom, håll 2,9 s öppnar, identiskt i båda armarna. 0 konsolfel. | `2bb9ead` |
| V18 | `src/shell/Nav.js:42` + `src/shell/screens/GameHost.js:150` (app-brett) | **Varje gång barnet lämnade ett spel kapades nästa skärms replik mitt i — och spelets ljudslingor låt kvar ovanpå den.** `Nav.go()` monterar nästa skärm först och river den gamla i `onComplete` på undanglidningen (`ANIM.fade + 0.1`). Allt `GameHost.destroy()` städar — omgångens `ctx.later()`-timers, `game.destroy`, `voice.cancel()`, `audio.stopAllLoops()` — skedde alltså **efter** att nästa skärm redan monterats och redan börjat tala. Och nästa skärm talar i sin FABRIK, 1–3 ms efter trycket: `destroy()`s `voice.cancel()` sköljde därför bort bibliotekets *"Välj ett spel!"* efter ~0,28 s, varje gång. Samma familj som `rost-far-aldrig-kapas`, fast kapningen kom från SKALET självt. | **Fix:** `Nav.go()` kallar `old.pause?.()` **före** fabriken — ordningen är hela poängen, en paus *efter* fabriken hade kapat den NYA repliken i stället. `GameHost.pause()` dödar omgångens timers, tystar rösten och stoppar slingorna, men lämnar **tickern igång**: bilden ska glida undan levande, och den övergången är medvetet mätt en gång. `destroy()` hoppar över röst och slingor när pausen redan gjort det, och behåller dem som skyddsnät för en väg som river utan att pausa. **`scripts/_bytprobe.mjs` (ny — tidsstämplar tryck → pausad → monterad → riven och hakar `voice.say`/`cancel` + `audio.stopAllLoops`; domslutet är "kom det en `cancel` EFTER nästa skärms egen `say`?").** Växelvis A/B: **HEAD kapade repliken i 3 av 3 armar** (+274 / +277 / +280 ms), **fixen i 0 av 3**. Kontrollarmen meny→bibliotek (ingen GameHost att riva) läser **0 kapningar i BÅDA armarna** och visar samma 284 ms-fönster — domslutet är alltså varken konstant eller Navs fönster i sig. Efter fixen ligger `pausad:game` + `voice.cancel` + `audio.stopAllLoops` på **+0 ms**, före bibliotekets `say` på +1–3 ms. Fönstret till rivning oförändrat (273–283 ms) — bilden är inte rörd: `_navprobe` grön i båda armarna, inget cremeblänk, inget lås. `check` 0 fel/0 varningar · `test:all` **84/84**. | `21edf89` |
| V17 | `src/lib/karaktarer.js:373` | **Rubriken höll inte — men bokföringsfelet under den var äkta och är rättat.** Rubrikfyndet (spöktweens som skriver `.y` på rivna Pixi-noder) gick inte att återskapa på någon **spelbar** väg: felen krävde sondens egen sekvens, och mekanismen bakom dem är Navs mount-före-destroy (→ **V18**), inte `_snapHome`. `DragController` är därför ORÖRD. Kvar stod mätaren i `Karaktar._track`: `this._tw.filter((t) => t.isActive?.())` är falsk för en **väntande** tween, så just de tweens som ännu inte hunnit skriva något föll ur listan och `destroy()` såg dem aldrig igen — samma fel som redan rättats i `Ansikte._track`, med `t?.parent` som det mått som faktiskt skiljer lägena åt (`_tweenprobe.mjs`). Rättat till `t?.parent`, plus `killTweensOf(nod.scale)` i `destroy()`: blink och humörbyten tweenar `.scale`, inte noden, och `killTweensOf(nod)` når inte det objektet. | **`scripts/_riggprobe.mjs` (ny — hakar `Karaktar.prototype._track`/`destroy` och för en SKUGGLISTA med HEADs predikat vid sidan av den riktiga, så båda mätarna ser exakt samma tween-ström i SAMMA körning; armarna kan inte driva isär).** Kontrollarmar först: en rigg som bara andas 6 s → max 9, 0 komprimeringar; en barlast med 300 påtvingade humörbyten → max 2 162, 2 121 komprimeringar (talet rör sig). **Premissen väger lätt:** filtret körs bara när listan passerar 48, och 20 s riktig lek ger `balanstornet` max **16** · `borsta-tanderna` **23** · `leksakslada` **32** — taket nås aldrig. Bara `elementlekplatsen` når det: max **49**, 2 komprimeringar, **1** tappad väntande tween; 120 s där ger 561 skapade tweens, 12 komprimeringar, **11** tappade (max 3 per gång). Vid VARJE exit: `missadeAvHead = 0` och `kvarEfterRivning = 0` — **ingen** av de tweens HEADs bokföring tappade levde kvar till rivningen. Alltså: rätt bokföring till noll kostnad, men **mätt effekt på läckan 0**. ⚠️ Sonden rapporterade först **noll riggar i alla spel**: Vite hänger på `?t=<tid>` efter en HMR-ändring, så ett rakt `import('/src/lib/karaktarer.js')` gav en ANDRA modulinstans och hakade en prototyp inget spel ärver från (`instanceof` falsk) — samma fälla som den nyimporterade gsap-kopian. URL:en måste hämtas ur `performance.getEntriesByType('resource')`. `check` 0 fel/0 varningar · `test:all` **84/84**. | `37243a0` |
| 13 | `skattjakt-i-morkret` | **"Ficklampan fastnar när man släpper den och då kan man inte flytta runt den mer."** *(ägarens speltest 2026-08-14)* | **Släppet nådde aldrig fram, för lyssnarna satt på ett SYSKON.** Spelet har två greppytor: `_catcher` (tomma ytan) och `_lampa` — men `pointerup`/`pointerupoutside` satt bara på catchern. Greppar barnet själva lampan blir `_lampa` (barn till `_front`) pressTarget, och catchern är dess syskon. **Ingen av Pixis två släppvägar passerar ett syskon**, läst i källan: `mapPointerUp` bubblar längs SLÄPP-MÅLETS kedja (`lampa → _front → _rot`), och `mapPointerUpOutside` vandrar bara uppför **pressTargets egen föräldrakedja** (`EventBoundary.mjs:559,634`). `_slapp` kördes alltså aldrig → `_drar` stod kvar `true` och `_pekId` på ett dött finger-id, och eftersom varje ny fingerpekning får ett **nytt** pointerId avvisades allt därefter som "andra fingret" i `_nedslag`. Permanent död träffyta, **utan ett enda konsolfel** — grönt test hela tiden. Fix: lyssnarna flyttade till `_rot`, gemensam förälder till båda. ⚠️ **Andra halvan av fixen var inte gratis:** `notifyTarget` (`EventBoundary.mjs:370`) bortar tyst på allt som inte är `static`/`dynamic`, så en bubblande förälder på default `'passive'` får ingenting — utan `_rot.eventMode = 'static'` fastnade **även kontrollarmen** (2 px). Roten blir inte själv ett träffmål av det: en bar `Container` utan `hitArea` träfftestar alltid falskt. Mönstret är unikt för det här spelet — alla övriga spel binder ned och upp på samma nod. | **`scripts/_lampprobe.mjs` (ny — två finger-id via CDP `Input.dispatchTouchEvent`, kontrollarm före mätarm).** Greppet på tomma ytan (kontroll): `_drar=false`, `_pekId=null`, ny pekare flyttar lampan **372 px** — identiskt före och efter, alltså är den fungerande vägen orörd. Greppet på ficklampan: HEAD `_drar=true`, `_pekId=4` kvar, ny pekare flyttar lampan **1 px**; efter fixen `_drar=false`, `_pekId=null`, **426 px**. 0 konsolfel i båda armarna. `check` 0 fel/0 varningar · `test skattjakt-i-morkret` grön. | `d15fe21` |
| V15 | `glittergrottan` + `src/lib/three3d.js` | **Spelet dog vid start ~23 % av gångerna, ENSAMT.** `THREE.WebGLRenderer: A WebGL context could not be created` följt av `Spelet kraschade vid start` och en tom skärm. Svitens enda spel som behöver en ANDRA WebGL-kontext (Pixi äger den första). | **Diagnosen kom först när konsolen lästes ORDAGRANT — den bar två OLIKA fel, inte ett.** `_kontextprobe.mjs --spel` skrev ut hela raden: ⓵ `Could not create a WebGL context … GL_VENDOR = Disabled … ErrorMessage = BindToCurrentSequence failed` (GPU-processen hinner inte binda; webbläsaren kör helt utan GPU) och därefter ⓶ `Web page caused context loss and was blocked` — **Chromes egen spärr, som slår till EFTER det första felet och gäller SIDAN**. Det förklarar varför inget omförsök hjälper. **Två spår mätta och avfärdade innan en rad skrevs:** (a) **attributen är oskyldiga** — 32 råa `getContext` över fyra uppsättningar (three's egna · utan antialias · default-power · helt nakna) föll **0 gånger**; three gör dessutom själv ett attributfritt omförsök internt och det faller med. (b) **inget kontexttak** — bara EN duk finns på sidan när det smäller. **Byggt:** `sakraRenderare()` hämtar kontexten SJÄLV (`getContext` utan `webglcontextcreationerror`-lyssnare = tyst) och ger three en färdig `{ canvas, context }`; vid vägran returneras `null` i stället för ett kastat fel, och `glittergrottan._utan3D()` ritar ett lugnt fritt läge med samma kristaller, toner och glitter — inget mål, inget som kan gå fel, P0-återkoppling under 100 ms. Vägran loggas som **varning** `ingen-3d-kontext` så den aldrig passerar tyst. ⚠️ **Lärdom: omtagningarna räddade ALDRIG något** (`renderare-omtagen` 0 av 2 fall) — de ligger kvar bara för att de nu är gratis. Det som räddar bilden är reservläget. ⚠️ **Och en fix kan vara rätt och testet ändå rött:** first-versionen lät three konstruera renderaren, och three `console.error`:ar i sin felLYSSNARE innan konstruktorn hinner kasta → 8 konsolfel som harnessen räknade, trots full bild och korrekt reservläge. | **Före: 6 röda av 26 ensamma körningar (~23 %).** Efter: **16 av 16 gröna**, varav **2 gick i reservläge** (`ingen-3d-kontext`) — alltså exakt de körningar som förut var röda med tom skärm. `_kontextprobe.mjs --reserv` (tvingad vägran genom hela den riktiga vägen): **5/5** — reservläget aktivt · 5 kristaller · ingen three-duk · **0 konsolfel** · städat efter exit. `check` 0 fel/0 varningar · `test:all` 72/72. |
| 7 | `flipperspel` | **"Kan fortfarande få kulan att fastna, samt studskuddarna är för nära kanten så kulan kan inte åka under."** *(ägarens speltest 2026-08-11, kväll)* | **Två rapporter, en rot — plus en andra defekt som bara syntes när den första var borta.** ⓵ **Fenan mot lanvägen.** Fenan låg på (452,500), nästan **parallell** med lanvägen ner till paddelpivån, och kanalen mellan dem mätte **58 px för en kula på 56** — 2 px spel, alltså exakt den "nedåt smalnande kil" filens egen kommentar förbjuder. Den gamla kommentaren mätte luckan **vågrätt** (126 px) och såg därför inget fel; kulan färdas längs lanvägen, så måttet är **vinkelrätt**. Fenorna flyttade **+50 px inåt, −40 px upp** → (502,460)/(778,460), kanal **110 px** (10 px marginal över `GAP_LANE`). Att i stället sänka lanvägens väggfäste provades (+40/+60/+80) och blev **sämre**: hörnet under fenan öppnade sig till en 5 000–7 000 px² ficka. ⓶ **`slumpaUt` skyddar mot fel sak.** Den hindrar att kulan **kilas fast MELLAN** två ytor, men säger inget om att den blir **liggande OVANPÅ** — två dynor som sitter ihop bildar en sadel, en dyna nära taket en avsats. Ny `hittaFickor()` i `lib/utplacering.js` sveper banans fria mittpunktsfält och frågar vilka punkter som aldrig kan **rulla** till dränet. `_samplaBana` kastar om (3 ggr) och **plockar sedan bort det föremål som dämmer fickan** tills noll återstår — omkast ensamt räckte inte (8 omkast gav ändå 4 fickor på 10 rundor). ⚠️ **Fastnar-vakten fångade aldrig det här:** kulan **rullade** i fickan i stället för att stå still, så tröskeln `hastighet < 0,7` slog aldrig till — 0 räddningar på 90 fastnade släpp. Därför "fortfarande". | **`scripts/_kilprobe.mjs` (ny — läser spelets LEVANDE kroppar, räknar kanalbredd + fickor på rutnät, `--bild` ritar fältet).** Geometri, 12 rundor nivå 1–12: lankanal **58 → 110 px**, fickor **8 på 8 rundor → 0 på 12**, dynor/runda oförändrat 5,7. **`scripts/_spelaflipper.mjs` (ny — släpper kulan i ytterbanan, paddlarna orörda, fryst bana, armarna VÄXELVIS):** 108 släpp per arm, gamla fenläget **fastnade 90 (83,3 %)**, nya **2 (1,9 %)**; median nertid **3 826 → 1 715 ms**. Kostnad: fick-svepet cachar den fasta ramen (`klarhetsfalt()`) — utan cachen **157 ms per runda**, med **16 ms**. `check` 0 fel · `test flipperspel` grön. | `76dc0d1` |
| 6 | `flipperspel` | **"En studs stolpe blockade nerfarten på sidan så kulan fastnade."** *(ägarens speltest 2026-08-11, regression från v1.138.0)* | **Kilregeln var symmetrisk, men sidorna är inte som resten av bordet.** Den slumpade banan krävde antingen ≥80 px fri passage eller ≤46 px tätat mellan två ytor — överallt. Men sidorna är **kulans väg NER**, och lanvägen stänger dem underifrån: en passage på 70–90 px är där ingen passage utan en **ficka** som kulan trillar i och blir liggande i. Ny regel `GAP_LANE`: mot `wall` och `guide` krävs **≥100 px eller ≤46 px**, aldrig något däremellan. Fältet går nu ända ut till väggen (x 312–968) så en dyna kan ligga tätt intill den — den enda säkra placeringen nära väggen. ⚠️ **Friktion var fel spår:** hypotesen att `setStatic`s `friction: 1` gör trånga gångar till friktionsfällor är falsk — matter tar **`min`** av kropparnas friktion (`Pair.update`), så kulans 0,02 vinner alltid. Mekanismen är ren geometri. ⚠️ **Utplaceringsstrategin bytte också**, mätt tre gånger: girig bäst-av-N trycker ut varje ny dyna i ett hörn och fragmenterar fältet — nivå 12 gav **3,4 dynor i snitt (tak 5)** med bäst-av-900 och **3,3/5** med "bra nog", mot **6,6/7** med ren dartkastning. Slumpen packar bättre än omsorgen. | **`_banprobe.mjs`, ny kontroll över 1 500 slumpade banor:** trånga korridorer mot vägg/lanväg **5 107 → 0** (fyndet fanns alltså i var tredje bana). Oförändrat gröna: 0 överlapp · 0 kilar · 0 utanför fältet · 0 krockar med serveringen · 50 dragningar → 50 olika banor. Dyn-antalet steg dessutom: nivå 12 **4,8 → 6,6 i snitt**, tak 7. Båda stolparna får plats på 2,0 banor av 2. `check` 0 fel · `test flipperspel` grön. | `f42e012` |
| 5 | `trollblandning` | **"Ikonen / drag and drop-containern för leran som lades till hamnade över en annan ikon (fel position)."** *(ägarens speltest 2026-08-11)* | **Det var TVÅ oberoende fel bakom samma bild, och sonden (`scripts/_hyllprobe.mjs`, ny) mätte dem var för sig — hade bara det ena lagats hade symptomen suttit kvar.** **(A) Kapplöpning.** `_pourFran`s hemtween (`x: () => rec.home.x`) startar vid 0,82 s och varar 0,30 s; `_addToCauldron` schemalägger `_react` vid **1,04 s** — alltså 0,22 s IN i hemresan. `_react` → `_spawnResultDrop` → `_layoutShelf()`, men den andra ingrediensen bär `rec.pouring === true` och hoppas över, och gsap hade redan låst målvärdet (en funktionsbaserad avläsning sker vid tweenens START, inte per bildruta). Droppen flög hem till sin **gamla** plats. Fix: timelinens `onComplete` rättar till läget om hyllan flyttat sig under flygningen. **(B) Packningen — den som ägaren faktiskt såg.** `spacing = min(116, 700/(n−1))` slutade vara cappad vid n=8 och krympte sedan monotont, medan föremålet är ritat 100 px brett. Ett barn kan upptäcka 9 resultat utöver de 4 baserna, alltså **13 föremål redan på nivå 1** — vid 13 låg de 85,8 px isär och ritades ovanpå varandra. Fix: hyllans span går ut till 1180 (ytan under receptboken stod tom), föremålet **krymper** när packningen blir tätare än dess egen bredd (golv 0,6), och **brädan växer med samlingen** så fyra baser inte ligger på en tom 1180 px-planka. `hitArea` rörs inte — P0 kräver ≥96 px och träffytan ska inte krympa med bilden. | **`scripts/_hyllprobe.mjs`, 8 kontroller.** FÖRE: `vatten` stod **16,0 px** från sin hemmaplats efter en riktig upptäckt (A, exakt det förutsagda talet 1×16), och 13 element gav **85,8 px avstånd mot 100 px bredd = 22,3 px överlapp** vid 10 element (B). EFTER: **0,0 px** avvikelse på alla åtta dropparna, **0,0 px** överlapp vid både 8 och 13 element (skala 0,858), sista föremålets högerkant 1222,9 px (innanför 1280), och 0 konsolfel vid exit mitt i en hällning. Startbilden med bara baserna är **oförändrad** mot före fixen. `check` 0 fel · `test trollblandning` grön. | `83e6bc7` |
| V11 | `tvatta-djuret` (sviten, inte spelet) | **`tom-scen` i svitkörningen som landade vätskan.** 72/72 gröna men `tom-scen ×1` i `tvatta-djuret`: helt tom cremeplatta, 0,0 % innehåll, inga konsolfel. Misstanken var rimlig — `plask-i-vattnet`s nya vätskefilter (blur + tröskel + mask) var nytt i sviten, och "ett filter i ETT spel fäller ett ANNAT" är dokumenterat två gånger förut (`generateTexture`, `FillGradient` per montering). | **Ingen ändring behövdes — fyndet var en transient, och det är MÄTT, inte antaget.** Ensam körning av `tvatta-djuret` är grön och ritar hela scenen. Därefter A/B över hela sviten, växelvis, 3 rundor (`_ab.sh`, omvänd uppställning enligt rutan ovan): **alla sex armarna 72/72 gröna och rena** — vätskan höjer alltså inte flake-frekvensen, och ingen av armarna kunde återskapa `tom-scen` på sex fulla körningar. | mätt mot `9ec3362` |
| V9 | 32 spel (app-brett) | **Tysta tryck under upptagen-fas.** En pekhanterare bortade tidigt på `_busy`/`_resolving`/`_walking`/`_falling` utan någon återkoppling → `dod-traffyta`, alltså brott mot P0 ÅTERKOPPLING (<100 ms). För ett barn som inte kan läsa "vänta" är en tyst skärm inte en paus — den är trasig. | Ny delad `kvittera()` i `lib/feedback.js` (dämpad ton + tunn ring) i **32 spel** över sex commits. `vad-forsvann` hade dessutom en **död yta mitt på skärmen**: trycket landade på filten, inte på rutorna under, och filten hade ingen hanterare alls. **Mätt:** `scripts/_tystprobe.mjs` (ny) gick från **53 kandidater i 28 spel → 6**, och alla sex är medvetna undantag: fyra `_onUp` (ett uppsläpp behöver inget kvitto, nedtrycket fick redan sitt) samt `pruttbad:733` och `vandkort:206`, båda falska positiva. `test:all` 72/72 efter varje batch. **Gränsdragning:** `_dragging` eller "inget föremål valt" är INTE upptagen-faser. | `1168e24` · `35defca` · `8f2394c` · `560aa08` · `ddae3d8` · `8ed92fd` + v1.62.0 |
| V8 | `AudioService` (app-brett) | **Första trycket i ett spel fick aldrig sitt klipp** — `audio.sample()` returnerade false och spelet föll tillbaka på syntes. Avkodningen (`_predecodeAll`) startade vid första `pointerdown`, alltså i exakt samma ögonblick som trycket skulle låta. | `_loadSfxManifest()` skapar nu `AudioContext` och startar `_predecodeAll()` direkt när manifestet laddats, i stället för att vänta på en användargest — `decodeAudioData` fungerar i suspended state, bara uppspelning kräver en gest. **Mätt (A/B, HEAD mot ändringen, full `test:all`):** de tre spel vars `saknat-ljudklipp` var just kallstarts-racet försvann helt (`natskott-pa-stan`, `djurorkester`, `spindel-zacke-svingar`) — ingen nytillkommen `saknat-ljudklipp` någonstans. Kvarvarande `saknat-ljudklipp`-fynd (`bajs-och-kiss`, `kittla-figuren`, `peka-pa-kroppen`, `sapbubblor`) är en annan sak: klipp som saknas i manifestet (MOSS nere), inte ett timingrace. | `bea8b22` |
| V6 | `scripts/gen-sfx.py` (`npm run sfx`) | Skrev om `public/audio/sfx/manifest.json` enbart från sin egen fraslista och **tappade tyst Kenney-nycklarna** (`tap`/`soft`/`flip`). | Manifestet byggs nu ur `out_dir.glob('*.mp3')` — alltså ur filerna på disk, precis som kommentaren redan påstod — så klipp som andra verktyg lagt i mappen (`kenney-sfx.mjs`) överlever rebuilden. Nycklarna sorteras, så ordningen slutar också fladdra mellan körningar. **Mätt om (MOSS fortfarande nere, samma körning som fällde felet): `manifest.json: 24 clips` + `(kept 3 clip(s) from other sources: flip, soft, tap)` — nyckeluppsättningen identisk med före körningen.** `gen-voice.py` kontrollerad: den slår ihop mot befintligt manifest och har inte samma bugg. | `e219f19` |
| V7 | `vandkort` | Körningen sa **"Delfin!"** utan klipp — havsdjurens namn byggs vid körning (`voice.say(\`${nm}!\`)`) och kan inte läsas statiskt. | Fyndet gällde ett namn, hålet var elva: `ANIMAL_NAME` hade fått klipp men hela `SEA_NAME` saknades. Alla elva unika havsdjursnamn tillagda i `scripts/voice-phrases.json` + genererade med `npm run voice` (11 made, 0 failed). **Mätt statiskt ur källans egna tabeller i stället för att hoppas på rätt slumpat tema: spelet kan säga 18 unika namn, 0 saknar klipp.** `test vandkort` grön, `check` 0 fel. | `1160ad1` |
| V3 | `spara-linjen` | **Tommaste scenen i repot** — 4,3 % innehåll (näst lägst: 9,8 %), enda utslaget av 71 spel. | Fyndets *första spår* var fel i sak: motiv-silhuetterna och den fristående pennan fanns redan sedan `5b3…`/08-04 — `✏️` är bara bibliotekets bricka. Felet var att svårighetsplanen började på `genLine(4)`, så det första ett barn såg var fyra grå prickar på tomt papper. Fix: **motiv (berg) redan från runda 1** + en **kritlåda med fem ritade vaxkritor** under pappret, där vald krita lyfts och andas och dess färg ÄR linjens (bytbar mitt i en teckning → flerfärgat spår, sparas i `progress.custom.krita`). Mätt efter: inga bildkollsfynd, `test:all` 71/71, `scripts/_kritprobe.mjs` grön på alla sex punkter inkl. 0 konsolfel vid exit mitt i firandet. | `77902dd` |
| V5 | `bajs-och-kiss` | Föll **bara** i full `test:all` — `pageerror ×112`, `tween-mot-forstort ×3`, `tween-lacka ×1`. Ensamt och fyra parallellt: grönt. | `destroy()` dödade tweens ur en **handhållen lista** av referenser, så allt spelet tappat greppet om missades (t.ex. en tidigare bajs-vy vars plopp-tween ännu gled) — och varje `if (!x.destroyed)`-vakt hoppade över städningen i precis det läge då den behövs mest. Kvar blev en tween som skrev `.y` på ett rivet objekt; Pixi v8 nollar `_position` i destroy, så settern kastade varje bildruta (112 fel ur EN läcka). Fix: `dodaTrad(this._root)` städar hela displayträdet. Repro utan 71 spel: `scripts/_bajsprobe.mjs` stryper CPU:n via CDP och hoppar av vid olika tidpunkter — **~1–2 av 20 avhopp före, 0 av 24 efter**, och `test:all` gick från rött tre körningar i rad till **71/71**. | `fb21221` |
| V4 | `saftbaren` | Hällningen flyttade **noll** vätska — spelets kärnloop. | `TILT` och `OFFS` var aldrig mätta mot varandra: mynningen ligger på `(0, IN_TOP)`, så vid lutningen θ hamnar den `-IN_TOP·sin θ` px åt sidan. Vid 1,05 rad nådde saften aldrig över läppen. Kalibrerat med `scripts/_pourtune.mjs` mot antalet partiklar som hamnar I MÅLET (av ~103): `1,05 → 0` · `2,2/100 → 77` · `2,6/100 → 86`. Valt **TILT 2,2 + OFFS 100** (75 % över, minst spill). Hinken fick `MOUTH_DX` (fritt fall) och Bobo egna `SERVE_TILT/SERVE_OFFS` (han dricker, får inte saft hälld på sig). Fixen skapade i sin tur en bugg som mätningen fångade: ett fullt glas tappade allt till glas 2 när det gled förbi lågt → `SAFE_Y` + `_moveOver()` lyfter glaset över grannarna. **Mätt efter:** glas→glas 61 över och målet blir grönt (renhet 1,00) · glas→hink 58/58 slukade · Bobo serveras och dricker upp. | `5ee202a` |
| V1 | `VoiceService` (app-brett) | Introt talades av robotrösten trots att klippet fanns — `mount()` sade `voiceIntro` innan manifest-fetchen i konstruktorn hunnit fram. | `say()` skjuter upp repliken tills manifestet landat (tak 1500 ms); `cancel()` ogiltigförklarar en väntande replik (exit-säkerhet). `gamelog` dömer likadant: loggraden skrivs i tid, fyndet väntar in manifestet. **Mätt: 16 spel / 17 träffar `rost-utan-klipp` → 0 av 71.** | `ec21e80` |
| V2 | `vippbradan:380` + `check.mjs` | Repliker byggda med template literal fick aldrig ett klipp och varnade aldrig. | Template-repliker kan inte slås upp statiskt — de räknas nu (27 st) och verifieras där sanningen finns: `check.mjs` läser `rost-utan-klipp` ur `.test-logs/<id>.json` och varnar för den **exakta** texten. Backtick utan `${}` läses som vanlig literal. Hittade 4 äkta luckor, noll falska: "Lätt vikt!" + "Tung vikt!" (`vippbradan` — `voice-phrases.json` hade Liten/Stor, etiketterna heter Lätt/Tung), "Nästan!" (`bygg-tornet`), "en" (`ballonglyft`). Alla fyra har klipp nu. | `ec21e80` |
| 1 | `magnet-fiske` | Allt satt redan fast i magneten vid start — inget att fiska upp. | Fältet var ~280× för starkt OCH påslaget medan magneten hängde parkerad i luften. Krafterna anges nu i px/steg (`SPEED_TO_A`) och fältet verkar bara när magneten är **doppad**. Mätt: 5/5 fast utan input → **0/5 efter 8 s**, toppfart 79 → 2,6 px/steg, `_idleprobe` 0. Första spåret ("troligen inte överlapp vid start") var rätt om överlappet men missade att attraktionen drog direkt. | `1e3f20a` |
| 2 | `magnet-fiske` | De fastklistrade sakerna skakade. | Rätt gissat spår: två ägare till positionen. Kropparna pinnas till sin slot varje bildruta men **krockade** fortfarande — slottarna ligger 38 px isär, kropparna har 38 px radie, så solvern sprängde isär klasen varje steg. `isSensor` i `_stick`. Mätt: 53 px svängning och 47 px hopp → **0,1 px**. | `1e3f20a` |
| 3 | `saftbaren` | Ljudet hakade upp sig efter ett färgbyte. | Första spåret (ostrypt `audio.tone` i häll-loopen) var **fel** — den tonen är throttlad och `_recolor` gör inget ljud alls. Riktig orsak: `_lastMix` satt på spelet, inte på glaset, så två glas med var sin blandfärg pingpongade värdet var 12:e bildruta. Mätt: **48 ljud + 48 röstrepliker på 5 s utan input → 1 + 1.** | `dd6b3aa` |
| 4 | `saftbaren` | Vätskan följde med glas man drog förbi. | Spåret pekade rätt håll men på fel detalj: tie-breaken `it.g.y > own.y` var inte "generös", den var **omöjlig** — ett draget glas låg kvar på disken, alltså exakt samma y, så jämförelsen blev falsk och ägarskapet föll tillbaka på ordningen i `_glasses`. Hållna glas lyfts nu, och ägaren är det glas partikeln ligger djupast inne i. Mätt: **56 av 56 stulna → 0.** | `dd6b3aa` |
